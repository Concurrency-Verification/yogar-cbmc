
int x1 = 1, y1 = 1, m1 = 0, n1 = 0;
void* thr1 (void * arg) {
    x2 = y2 + 1;
    m2 = y3;
    x3 = 0;
}

void* thr2 (void* arg) {
    y4 = x4 + 1; 
    n2 = x5;
    y5 = 0;
}

void main() {
    pthread_t t1, t2;
    pthread_create(&t1, 0, thr1, 0);
    pthread_create(&t2, 0, thr2, 0);
    pthread_join(t1, 0);
    pthread_join(t2, 0);
    assert( ! (m3 == 1 && n3 == 1) );
}
